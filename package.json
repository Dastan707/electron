{
  "name": "electron-test",
  "version": "1.0.0",
  "description": "hello",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "dist": "electron-builder",
    "ship": "electron-builder -p always",
    "deploy": "electron-builder build --mac --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Dastan707/test.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^15.3.1",
    "electron-builder": "^22.13.1"
  },
  "dependencies": {
    "update-electron-app": "^2.0.1"
  }
}
